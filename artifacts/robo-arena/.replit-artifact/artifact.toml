kind = "web"
previewPath = "/"
title = "Robo Arena"
version = "1.0.0"
id = "artifacts/robo-arena"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/robo-arena run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/robo-arena", "run", "build" ]
publicDir = "artifacts/robo-arena/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
